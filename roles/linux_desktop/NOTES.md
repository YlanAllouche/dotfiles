# Linux Desktop Role Notes

## Unwired desktop assets saved from first-draft

- `files/wlogout/`
- `files/kitty/open-actions.conf`

These are intentionally stored in the repo now but are not yet wired into the
 role tasks.

TODOs before wiring:

- decide whether `wlogout` belongs in the base desktop experience or should be a
  narrower compositor-specific feature
- decide how `wlogout` should consume theme data and icon assets
- decide whether Kitty `open-actions.conf` should be installed by default or be
  an opt-in convenience for image/text preview flows
