# LLM TODO

- decide whether this instance uses plain env vars, `rbw`, or `pass`
- keep provider keys consumer-owned
- use `{{ consumer_contract_env_dir }}/llm.env` only for non-interactive scripts when needed
- prefer shell snippets for interactive session-only values
