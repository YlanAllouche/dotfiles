- import_playbook: "{{ ((lookup('env', 'DOTFILES_PUBLIC_REPO_DIR') | default('', true)) or (lookup('env', 'HOME') ~ '/workspaces/repos/github/YlanAllouche/dotfiles')) ~ '/playbooks/main.yml' }}"
  # Could also be: ../playbooks/main.yml
  vars:
    dotfiles_profile: custom
    # Optional structured consumer config is auto-loaded from:
    #   {{ playbook_dir }}/vars/dotfiles.consumer.yml
    # dotfiles_active_user: your-user
    # dotfiles_active_home: /home/your-user
