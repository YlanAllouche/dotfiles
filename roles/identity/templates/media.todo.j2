# Media TODO

- decide whether Jellyfin/Kodi values come from env files or runtime secret lookup
- use `{{ consumer_contract_env_dir }}/media.env` only when the helper script needs non-interactive values
- keep player/device-specific values local to the machine
