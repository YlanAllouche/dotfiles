shell_arch_packages:
  - oh-my-posh
  - zsh
  - zsh-antidote

shell_homebrew_packages:
  - antidote
  - eza
  - fd
  - fzf
  - oh-my-posh
  - zsh

shell_zsh_path: "{{ '/bin/zsh' if is_macos else '/usr/bin/zsh' }}"
