# Email TODO

- render account-specific `mbsync`, `msmtp`, and `neomutt` config from the consuming playbook
- keep secrets out of this repo
- prefer native config files over env vars for mail tooling
