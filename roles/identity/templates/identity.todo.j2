# Identity TODO

- decide which machines should set git name/email here
- decide where ssh keys are provisioned from
- decide whether `rbw`, `pass`, or native app config is preferred per machine
- decide how rclone config is injected on this instance
