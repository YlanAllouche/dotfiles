return {
	"R-nvim/R.nvim",
	-- Only required if you also set defaults.lazy = true
	lazy = false,
	-- TODO: if there's no languageserver instructions, have some sane defaults like the length line being somewhat illimited
}
