return {
	-- TODO: revisit editor-native LLM flows once the public/private split is clearer.
	-- Keep provider credentials in a private layer, eg:
	--   api_key = "{file:~/.config/codecompanion/<provider>-api-key}"
}
